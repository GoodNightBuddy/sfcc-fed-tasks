<isdecorate template="common/layout/page">
    <isscript>
        var assets = require('*/cartridge/scripts/assets.js');
        assets.addJs('/js/productTile.js');
        assets.addCss('/css/homePage.css');
    </isscript>
    <isinclude url="${URLUtils.url('Basket-Show', 'include', 'true')}"/>
    <p>Use of prepend/append/replace using viewData: ${pdict.param1}</p>

    <p>Use of extending cache.js (middleware script): ${pdict.param2}</p>
</isdecorate>
